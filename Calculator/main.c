#include <stdio.h>
#include "main.h"
/**
  * main - add, subtract and multiply
  * Return: Always 0
  */
int main()
{
	int i, j;
	
	for (i = 0; i < 3; i++)
	{
		printf("Enter number:\n");
		scanf("%d\n", &i);
	}
	if (i < 3)
	{
		return (i);
	}
	else 
	j = SUM; 
	printf("%d\n", j);
	return (0);
}
